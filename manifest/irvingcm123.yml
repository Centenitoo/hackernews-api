piVersion: apps/v1
kind: Deployment
metadata:
  name: irvingcm123
spec:
  replicas: 1
  selector:
    matchLabels:
      app: irvingcm123
  template:
    metadata:
      labels:
        app: irvingcm123
    spec:
      containers:
      - image: okteto.dev/irvingcm123:latest
        name: adsoftsito
        envFrom:
        - configMapRef:
            name: irvingcm-config
        livenessProbe:
          httpGet:
            path: /ht/
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---

apiVersion: v1
kind: Service
metadata:
  name: adsoftsito
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP
  ports:
  - name: "adsoftsito"
    port: 8080
  selector:
    app: adsoftsito